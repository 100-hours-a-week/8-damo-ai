name: Publish agent-dialogue image

on:
  workflow_dispatch:
  push:
    branches: [dev]
    paths:
      - 'services/agent_dialogue/**'
      - 'shared/**'
      - 'requirements.txt'
      - 'Dockerfile'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ github.workspace }}/services/agent_dialogue/Dockerfile.runpod
          push: true
          platforms: linux/amd64
          tags: |
            ghcr.io/kt20201224/agent-dialogue:latest
            ghcr.io/kt20201224/agent-dialogue:v1.0.${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
