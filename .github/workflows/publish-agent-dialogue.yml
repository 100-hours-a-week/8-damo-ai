name: Publish agent-dialogue image

on:
  push:
    branches: [ feature/v3/agent_dialogue ]
    paths:
      - 'services/agent_dialogue/**'
      - 'shared/**'
      - 'requirements.txt'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve image tag
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> "$GITHUB_ENV"

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/agent_dialogue/Dockerfile.runpod
          push: true
          platforms: linux/amd64
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/agent-dialogue:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/agent-dialogue:${{ env.IMAGE_TAG }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/agent-dialogue:cache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/agent-dialogue:cache,mode=max
