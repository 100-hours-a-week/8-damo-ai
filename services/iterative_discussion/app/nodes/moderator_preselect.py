from services.iterative_discussion.app.engine.state import ConsensusState


def moderator_preselect(state: ConsensusState) -> dict:
    """Node 2: 후보 식당을 점수 기반으로 Top 10 사전 선별 (stub)."""
    return {}
